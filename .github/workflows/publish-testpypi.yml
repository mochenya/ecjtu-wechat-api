name: 🧪 发布 dev 版到 TestPyPI

on:
  push:
    branches:
      - dev

permissions:
  contents: read
  id-token: write

jobs:
  publish-testpypi:
    runs-on: ubuntu-latest
    steps:
      - name: 📥 检出完整历史代码
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true

      - name: 🔄 同步项目依赖
        run: uv sync

      - name: 🔢 计算开发版本号
        id: version
        run: |
          # 读取原始版本号，并清理可能的后缀
          RAW_VERSION=$(grep '^version = ' pyproject.toml | sed 's/version = "\(.*\)"/\1/' | tr -d '"' || echo "")

          # 去掉任何 +local / .dev / .post 后缀，只保留主版本部分
          BASE_VERSION=$(echo "$RAW_VERSION" | sed 's/[+.+].*//' | sed 's/\.dev.*$//' | sed 's/\.post.*$//' | sed 's/\.a[0-9]*$//' | sed 's/\.b[0-9]*$//' | sed 's/\.rc[0-9]*$//')

          # 检查是否为空或明显不合法
          if [ -z "$BASE_VERSION" ]; then
            echo "错误：无法从 pyproject.toml 中读取有效的 version"
            echo "当前读取到的原始内容：'$RAW_VERSION'"
            exit 1
          fi

          # 简单检查：至少要有 x.y.z 这种形式（最宽松检查）
          if ! echo "$BASE_VERSION" | grep -qE '^[0-9]+(\.[0-9]+){1,2}$'; then
            echo "错误：解析出的基础版本 '$BASE_VERSION' 格式不合法"
            echo "必须是类似 0.1.0 / 1.2.3 / 2.0 这样的格式"
            exit 1
          fi

          # 使用 github run number 作为 dev 后缀（最推荐）
          NEW_VERSION="${BASE_VERSION}.dev${{ github.run_number }}"

          echo "version=${NEW_VERSION}" >> "$GITHUB_OUTPUT"
          echo "📦 开发版本号: ${NEW_VERSION}"

      - name: ✏️ 更新版本号
        run: |
          sed -i "s/^version = \".*\"/version = \"${{ steps.version.outputs.version }}\"/" pyproject.toml
          echo "✅ 版本已更新: $(grep '^version =' pyproject.toml)"

      - name: 🏗️ 构建项目
        run: uv build

      - name: 🚀 发布到 TestPyPI
        run: |
          uv publish \
            --publish-url https://test.pypi.org/legacy/ \
            --check-url https://test.pypi.org/simple/ \
            dist/*